type Company {
  id: ID!
  name: String!
  bio: String!
  staffMembers: Int!
  jobs: [Job!]!
}

type Job {
  id: ID!
  title: String!
  description: String
  company: Company!
  city: String!
  salary: Int!
}

type Query {
  job(id: ID!): Job
  jobs: [Job!]!
  company(id: ID!): Company
}
